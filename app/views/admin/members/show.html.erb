      <h2>posts</h2>
       <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th>
            </th>
            <th>Title</th>
            <th>Opinion</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <% @posts.each do |post| %>
        <tbody>
          <tr>
            <td> <%= post.created_at.strftime('%Y/%m/%d') %><%= post.member.name %><br>
            <%= post.title %><br></td>
            <td></td>
            <td></td>
            <td></td>
            <td><%= link_to "Edit", edit_admin_post_path(post.id), class:"btn btn-success"%></td>

          </tr>
        <% end %>
        </tbody>
      </table>