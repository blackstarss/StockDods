     <table>
	    <tbody>
        <% genres.each do |genre| %>
	  	  <tr>
          <td><%= link_to genre_path(genre) do %><%= genre.name %><% end %></td>
	 	  	</tr>
    	  <% end %>
    	</tbody>
    </table>