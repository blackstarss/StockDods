<table class='table'>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>
        <% posts.each do |post| %>
  <tbody>
    <tr>
      <td>
        <%= post.created_at %><%= post.name %><br>
        <%= link_to post_path(post.id) do %><%= post.name %><% end %><br>
      </td>

     <!--　　<td id="favorite_buttons_<#
     %= post.id %>">-->
　　  　<!--<%#= render partial: "favorites/favorite", locals: {post: post} %>-->
　   <!--　</td>-->
     <!--   <td>コメント数：<#%= post.post_comments.count %></td>-->

	  </tr>
  	  <% end %>
    /tbody
</table>
